{
  "name": "memebro",
  "version": "0.1.0",
  "private": true,
  "description": "AI-assisted meme generator — CSE 110 Spring 2026 final project (Group 19).",
  "type": "module",
  "scripts": {
    "start": "node src/backend/server.js",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "eslint .",
    "test": "node --test tests/export.test.js tests/image-loader.test.js tests/meme-canvas.test.js tests/template-filter.test.js tests/templates.test.js",
    "fix": "prettier --write . && eslint . --fix",
    "check": "npm run format:check && npm run lint && npm test"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.4.1",
    "globals": "^17.6.0",
    "prettier": "^3.3.3"
  }
}
